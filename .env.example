# Copy to .env and fill in. Never commit .env.
# Google Cloud Console: enable Maps JavaScript API (map), Directions API (routes), Generative Language API (Gemini).
# On web, CORS blocks Directions + Gemini from the browser. Run the proxy and set PROXY_BASE:
# Backend (wander/backend) reads MONGO_URI and JWT_SECRET from this file.

MONGO_URI=
JWT_SECRET=
EXPO_PUBLIC_GOOGLE_MAPS_KEY=
EXPO_PUBLIC_GOOGLE_DIRECTIONS_KEY=
EXPO_PUBLIC_GEMINI_KEY=
# For web only: EXPO_PUBLIC_PROXY_BASE=http://localhost:3001 (run: node scripts/proxy-server.mjs). On iOS leave unset.
EXPO_PUBLIC_PROXY_BASE=
